# TableChat Backend Environment Configuration

# =============================================================================
# LLM Configuration (普通模式 - 自然语言生成 SQL)
# =============================================================================
# 支持 OpenAI 兼容 API
LLM_API_BASE=https://api.openai.com/v1
LLM_API_KEY=your-openai-api-key
LLM_MODEL=gpt-4o-mini

# =============================================================================
# Agent 模式配置 (Claude Agent SDK)
# =============================================================================
# Agent API 端点 (可以是自定义代理)
AGENT_API_BASE=http://localhost:3000/api
# Agent 认证 Token
AGENT_API_KEY=your-anthropic-auth-token
# Agent 使用的模型
AGENT_MODEL=claude-sonnet-4-5
# Agent 最大对话轮数
AGENT_MAX_TURNS=20
# Agent 请求超时时间（秒）
AGENT_TIMEOUT=120

# =============================================================================
# Server Configuration
# =============================================================================
HOST=0.0.0.0
PORT=7888
DEBUG=false

# =============================================================================
# Database Timeouts
# =============================================================================
PG_CONNECT_TIMEOUT=10
MYSQL_CONNECT_TIMEOUT=10
